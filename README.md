# animation
This project demonstrates sequential animations using the Web Animations API in JavaScript. Three images are animated one after another, where each animation only starts after the previous one has completed.
